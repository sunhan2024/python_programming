# Part Two Features
Below are a list of features that are to be completed for the second part of the assignment. Refer to the Assessment section of the course page for more detail on each feature.

Please fill in the section for each feature replacing all text below each heading.

## 1. Validate user input: Crash Severity Report
Replace me with an outline of:

- how you implemented this feature
I added a function which is called 'read_int' to validate that the user input for year and spead_limit should be an integer.
- choices you made and why
I chose to add 'read_int'.
The reason is that the input should be integers rather than some float numbers or strings.

## 2. Use temporary speed limit if defined instead of normal speed limit
Replace me with an outline of:

- how you implemented this feature
If we would like to define a speed limit, in the main function, we change the assignment "speed_limit = read_int("Speed Limit: ")" into speed_limit = 50, for example.
- choices you made and why
The reason is that the above assignment is the only parameter of speed limit that is taken into report generation. If we want to define some temporary speed limit, we change it.

## 3. Warn the user if no records are found
Replace me with an outline of:

- how you implemented this feature
- choices you made and why

## 4. Add all years
Replace me with an outline of:

- how you implemented this feature
- choices you made and why

## 5. Implement Crash Reports Over Time Graph
Replace me with an outline of:

- how you implemented this feature
- choices you made and why
